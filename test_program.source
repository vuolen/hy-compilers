{
    while f() do {
        x = 10;
        y = if g(x) then {
            x = x + 1;
            x
        } else {
            g(x)
        };  # <-- (this semicolon will become optional later)
        g(y);
    };  # <------ (this too)
    123
}